<% if flash[:notice] %>
  <div class="flash">
    <%= flash[:notice] %>
  </div>
<% end %>

<table>
  <tr>
    <td><h4>Title:</h4></td>
    <td><%= @book.title %></td>
  </tr>

  <tr>
    <td><h4>Body:</h4></td>
    <td><%= @book.body %></td>
  </tr>
</table>

<%= link_to 'Edit', edit_book_path(@book.id) %>|<%= link_to 'Back', books_path %>